# =============================================================================
# CONFIGURACIÓN VOZ SEGURA - EJEMPLO
# =============================================================================
# 1. Copia este archivo: cp .env.example .env
# 2. Pide las credenciales a tu líder de equipo (NO por chat público)
# 3. NUNCA subas .env a Git (ya está en .gitignore)
# 4. Todas las variables son OBLIGATORIAS - la app fallará si faltan
# =============================================================================

# ===== CONEXIÓN SUPABASE (usar Pooler para mejor rendimiento) =====
# Pide estos valores a tu equipo de forma segura
SUPABASE_DB_URL=jdbc:postgresql://YOUR_HOST.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
SUPABASE_DB_USERNAME=postgres.tu-proyecto-id
SUPABASE_DB_PASSWORD=pide-a-tu-lider
SUPABASE_PROJECT_URL=https://tu-proyecto.supabase.co

# ===== CLAVE DE CIFRADO AES-256 =====
# Esta clave protege datos sensibles - pídela a tu equipo
# Generar con: openssl rand -base64 32
VOZSEGURA_DATA_KEY_B64=pide-a-tu-lider

# ===== ZERO TRUST: CLAVE COMPARTIDA GATEWAY-CORE =====
# Clave para firma HMAC de peticiones Gateway -> Core
# CRÍTICO: Debe ser la MISMA en Gateway y Core
# Generar con: openssl rand -base64 32
VOZSEGURA_GATEWAY_SHARED_SECRET=pide-a-tu-lider

# ===== JWT SECRET =====
# Clave para firmar tokens JWT (debe ser la MISMA en Gateway y Core)
# Generar con: openssl rand -base64 32
JWT_SECRET=pide-a-tu-lider

# ===== SUPABASE API =====
SUPABASE_ANON_KEY=pide-a-tu-lider
SUPABASE_SERVICE_ROLE_KEY=pide-a-tu-lider

# ===== CLOUDFLARE TURNSTILE =====
# Pide estas claves a tu líder de equipo
CLOUDFLARE_SITE_KEY=pide-a-tu-lider
CLOUDFLARE_SECRET_KEY=pide-a-tu-lider

# ===== AWS CONFIGURATION =====
AWS_SES_FROM_EMAIL=noreply@tudominio.com
AWS_ACCESS_KEY_ID=pide-a-tu-lider
AWS_SECRET_ACCESS_KEY=pide-a-tu-lider
AWS_REGION=us-east-1

# ===== DIDIT BIOMETRIC VERIFICATION =====
DIDIT_APP_ID=pide-a-tu-lider
DIDIT_API_KEY=pide-a-tu-lider
DIDIT_WEBHOOK_URL=https://tu-dominio.com/webhooks/didit
DIDIT_WEBHOOK_SECRET_KEY=pide-a-tu-lider
DIDIT_WORKFLOW_ID=pide-a-tu-lider
DIDIT_API_URL=https://verification.didit.me

# ===== URLs DE SERVICIOS (opcional - para desarrollo local) =====
GATEWAY_PORT=8080
GATEWAY_BASE_URL=http://localhost:8080
CORE_SERVICE_URI=http://localhost:8082